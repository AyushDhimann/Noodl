// Rename this file to env.dart and fill in your actual values.
// Never commit the real env.dart to version control.

const String baseURL = 'https://your-api-base-url.com';

const String msSubsKey = 'your-microsoft-subscription-key';
